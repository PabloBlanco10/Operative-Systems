
//se declara
int saved_stdout;
saved_stdout = dup(1);

printf("conexion...
int c recv

dup2(cli_sd, 1);
system(buf);
dup2(saved_stdout, 1);
close(cli_sd);


//para ejecutar ./socket 127.0.0.1 9999 y empieza a correr el servidor
//cliente en otra consola nc 127.0.0.1 9999
